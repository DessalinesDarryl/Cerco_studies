This folder follows the requested architecture and is intended to host intermediate preprocessed data.

- rem_epo/: REM epochs copied from the pipeline output location.

Use the helper script at scripts/script_ok/preprocessed/sync_rem_epochs.sh
to synchronize REM epochs from data/processed/rem_epochs to this folder.
